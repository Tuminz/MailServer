-Thuộc tính của đối tượng chỉ thay đổi bởi hành vi của nó.
-protected: bảo vệ, chỉ mở bởi các lớp con
-class diagram:
gồm 3 phần:(1 phần Cat theo quy tắc Pascal Case)
private: dấu trừ
public: dấu cộng
tên biến kiểu lạc đà (camelCase)
biến private có dấu gạch dưới
:: toán tử phân giải phạm vi
//Forward declaration (vd: class Cat;): phá tham chiếu vòng, thường đặt trong main.h
- Hằng số PI là công khai => dấu cộng, hằng số phải có gạch dưới.
- Hàm tạo: trùng tên lớp (Cat())
- Hàm hủy: trùng tên lớp nhưng thêm dấu ngã (~Cat())
- Hàm tạo và hàm hủy có thể tự động đc tạo và gọi.
- Con trỏ this trỏ tới đối tượng đang giữ địa chỉ
- tên hàm bắt đầu bằng động từ. Nếu bắt đầu bằng danh từ thì là hàm getter
-Tránh truy cập trực tiếp thuộc tính (attribute) --> getter/setter